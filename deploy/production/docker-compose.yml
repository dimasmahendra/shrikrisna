# docker-compose.yml

version: "3.7"
services:
  tailor_website:
    container_name: tailor_website
    restart: unless-stopped
    image: registry.gitlab.com/mectek/penjahit:production
    network_mode: host
    volumes:
      - /var/www/tailor/storage:/var/www/storage